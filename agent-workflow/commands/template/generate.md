管理責任: なし。Generatorが未実装であることを報告して終了します。
ワークフロー: (将来のbuild相当。`_template`のGenerator機構が移植された後に有効化します)
状態確認先: なし

権限: `read-only`
副作用: なし
承認: 不要です。

指定Profileでプロジェクトを試し生成します(未実装スタブ)。

## 読み込む文書

なし。

## 手順

1. 「Generatorは未実装です。`~/Projects/_template`のGenerator機構(`.claude/commands/template/`)が
   nix-stationへ移植された時点で有効化します」と報告して終了します
2. 現時点で利用できる代替(`~/Projects/_template`を直接使う、または`docs/draft/`の
   構想を手動で参照する)を案内します

---

次のステップ:

1. `/status` — 現在地の確認
