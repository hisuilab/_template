# .github/ISSUE_TEMPLATE

## 1. 概要

GitHubのIssueフォームテンプレートを置くディレクトリです。GitHubがこのディレクトリ名を規約として認識し、Issue作成時の選択肢として表示します。

## 2. 責任

- 保持するもの: Issueフォーム定義(`bug_report.yml` `feature_request.yml` `task.yml`)、白紙Issueの可否設定(`config.yml`)

## 3. 責任外

- Issue運用のルール(コミット規約等は`AGENTS.md`が持ちます)
