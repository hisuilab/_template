# template/parts/starter/web-api

## 1. 概要

Web API サービス向けの `src/` 骨格（`app.py`・`routes/`）を提供する starter/web-api Part です。

## 2. 責任

- `part.toml` によるメタデータと依存宣言
- `payload/` 配下の生成対象ファイル群（実装済み）

## 3. 責任外

- CLI・ライブラリ向けの骨格（他 starter Part が担当）
