[project]
name = "{{project_name}}"
version = "0.1.0"
description = "{{project_name}} web API service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "python-dotenv>=1,<2",
  "uvicorn[standard]>=0.35,<1",
]
