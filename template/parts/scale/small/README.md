# template/parts/scale/small

## 1. 概要

小規模プロジェクト向けのドキュメント骨格（`docs/draft/`）を提供する scale/small Part です。

## 2. 責任

- `part.toml` によるメタデータと依存宣言
- `payload/` 配下の生成対象ファイル群（M2 以降で実装）

## 3. 責任外

- 他スケール（tiny / medium / large）の提供（M3 以降）
