# template/parts/features/ai-agent

## 1. 概要

AI エージェント向け設定ファイル（`AGENTS.md`・`CLAUDE.md`）を追加する features/ai-agent Part です。

## 2. 責任

- `part.toml` によるメタデータと依存宣言
- `payload/` 配下の生成対象ファイル群（M2 以降で実装）

## 3. 責任外

- AI エージェント以外の features（他 features Part が担当）
