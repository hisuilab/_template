# template/parts/purpose/cli

## 1. 概要

CLI ツール向けの `src/` 骨格とエントリポイント（`src/main.py`）を提供する purpose/cli Part です。

## 2. 責任

- `part.toml` によるメタデータと依存宣言
- `payload/` 配下の生成対象ファイル群（M2 以降で実装）

## 3. 責任外

- Web API・ライブラリ向けの骨格（他 purpose Part が担当）
