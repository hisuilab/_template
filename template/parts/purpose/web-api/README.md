# template/parts/purpose/web-api

## 1. 概要

Web API サービス向けの `src/` 骨格（`app.py`・`routes/`）を提供する purpose/web-api Part です。

## 2. 責任

- `part.toml` によるメタデータと依存宣言
- `payload/` 配下の生成対象ファイル群（M2 以降で実装）

## 3. 責任外

- CLI・ライブラリ向けの骨格（他 purpose Part が担当）
