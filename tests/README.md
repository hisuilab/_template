# tests

## 1. 概要

テストを層ごとに分けて置くディレクトリです。`just test`(`bats --recursive tests/unit`)がユニット層を実行します。

## 2. 責任

- 保持するもの: `unit/`(ユニットテスト)、`integration/`(結合テスト)、`e2e/`(エンドツーエンドテスト)のディレクトリ分割

## 3. 責任外

- 各層のテストコード自体の責任範囲(それぞれのREADMEが持ちます)
