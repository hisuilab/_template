# tests/integration

## 1. 概要

複数モジュール・複数ツールの連携(例: justfile経由でのスクリプト呼び出し、devShellとpre-commit hookの結線)を検証する結合テストを置くディレクトリです。現時点ではディレクトリ雛形のみで、テストは未着手です。

## 2. 責任

- 保持するもの: モジュール間の結線を検証する結合テスト(今後追加)

## 3. 責任外

- 単一スクリプトの単体挙動(`tests/unit/`が持ちます)、生成先プロジェクトを跨ぐエンドツーエンド検証(`tests/e2e/`が持ちます)
